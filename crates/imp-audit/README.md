# imp-audit

Typed configuration, requirement discovery, normalized findings, doctor reports, and summaries for imp's audit subsystem.

This crate currently contains the audit domain model and validation layer. Runtime tool execution and agent integration belong in `imp-core`.
