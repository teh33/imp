[package]
name = "imp-process"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Typed subprocess lifecycle and supervision for imp"
publish = false

[dependencies]
thiserror.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
